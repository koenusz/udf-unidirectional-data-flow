name: udf
description: A package to implement a unidirectional data flow in your app.
version: 0.0.26
homepage: https://github.com/koenusz/udf

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.0

flutter:
